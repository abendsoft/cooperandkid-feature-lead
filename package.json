{
  "name": "cooperandkid-feature-lead",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit",
    "build": "node -e \"require('fs').copyFileSync(require('path').join('public','index.html'), require('path').join('index.html'))\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
